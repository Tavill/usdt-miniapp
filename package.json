{
  "name": "usdt-miniapp",
  "version": "1.0.0",
  "description": "Telegram Mini App — USDT → EUR Calculator",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
